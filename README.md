# cryptographic-output-watermarking
 embed an invisible statistical signature into every LLM response. If someone trains a model on stolen outputs and that clone appears, you can mathematically prove the clone was distilled from your system.
